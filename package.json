{
  "name": "sn777-gaming-portal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "mkdir -p dist && cp -r dist_backup/* dist/ && node patch_cloud_run_domain.cjs && npx esbuild server.ts --bundle --platform=node --target=node20 --format=cjs --packages=external --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "lint": "echo 'No linting needed'"
  },
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node-cron": "^3.0.11",
    "acorn": "^8.18.0",
    "cors": "^2.8.5",
    "esbuild": "^0.21.3",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "tsx": "^4.10.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
